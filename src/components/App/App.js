import React from 'react';
import PropTypes from 'prop-types';
import {
  Article
} from '../../components';

const App = (props) => (
  <div>
    <h1>Awesome Article Viewer</h1>
    <Article />
  </div>
);

App.propTypes = {

};

export default App;
