import React from 'react';
import PropTypes from 'prop-types';
import {
  Heading,
  Body
} from '../../components';

const Article = (props) => (
  <div>
    <Heading />
    <Body />
  </div>
);

Article.propTypes = {

};

export default Article;
